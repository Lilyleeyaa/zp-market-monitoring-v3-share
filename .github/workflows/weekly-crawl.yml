name: Weekly Crawl and Rank

on:
  schedule:
    # 매주 금요일 06:00 KST (UTC 21:00 목요일)
    - cron: '0 21 * * 4'
  
  # 수동 실행 가능
  workflow_dispatch:

jobs:
  crawl-and-rank:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run Weekly crawler
        env:
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
          GENAI_API_KEY: ${{ secrets.GENAI_API_KEY }}
        run: |
          python scripts/crawl_naver_news_api.py
      
      - name: Merge user feedback into training labels
        run: |
          python scripts/merge_feedback.py
      
      - name: Train LightGBM Model
        run: |
          python scripts/train_lgbm_model.py
      
      - name: Rank articles with LGBM
        env:
          GENAI_API_KEY: ${{ secrets.GENAI_API_KEY }}
        run: |
          python scripts/rank_articles.py
      
      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/articles_raw/*.csv
          git add data/labels/*.csv
          git add data/labels/feedback_archive/ || true
          git add model/*.pkl model/*.txt
          git diff --staged --quiet || git commit -m "Auto: Weekly crawl + feedback merge $(date +'%Y-%m-%d %H:%M')"
          git push origin HEAD:${{ github.ref_name }}

