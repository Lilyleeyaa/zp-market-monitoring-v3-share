# ZP Market Monitoring v3 - Authentication Configuration
# 이 파일은 .gitignore에 포함되어 GitHub에 업로드되지 않습니다

# 공통 비밀번호 (SHA-256 해시)
# 공통 비밀번호 (SHA-256 해시)
common_password_hash: "9af8e589e496a744e10a0eb1013a767976bb63e2f39dbb32c4e40b81506f5e8d"

# 내부 사용자 이메일 도메인
# 이 도메인의 이메일은 Internal Weekly 대시보드에 접근 가능
internal_domains:
  - "zuelligpharma.com"

# 사용 방법:
# - Internal Weekly: @zuelligpharma.com 또는 @zp.com 이메일만 접근 가능
# - External Weekly: 모든 이메일 접근 가능 (비밀번호만 맞으면)
# - Daily Validation: 모든 이메일 접근 가능 (비밀번호만 맞으면)
#
# 비밀번호: 모두 동일 (ZPzp!)
# 변경 시: auth/generate_password.py 실행하여 새 해시값 생성
